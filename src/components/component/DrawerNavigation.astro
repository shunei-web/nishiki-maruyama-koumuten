---
interface Props {
  menus: {
    name: string;
    url: string;
  }[];
}

const { menus } = Astro.props;
---

<nav id="js-drawer" class="drawer-navigation">
  {
    menus.map((item) => (
      <a href={`${item.url}`} class="drawer-navigation__link">
        {item.name}
      </a>
    ))
  }
</nav>

<label for="js-hamburger" class="drawer-navigation__overlay"></label>

<style lang="scss">
  .drawer-navigation {
    @include global.mq() {
      display: block flex;
      column-gap: calc(40 * var(--rem-ratio));
    }

    &.is-active {
      display: block grid;
      row-gap: calc(41 * var(--rem-ratio));
      padding-block: calc(10 * var(--rem-ratio)) calc(40 * var(--rem-ratio));
      background: var(--color-white);
    }
  }

  .drawer-navigation__link {
    text-align: center;
    font-size: var(--font-size-30);
    line-height: var(--line-height-30-36);

    @include global.mq() {
      font-size: var(--font-size-20-30-bp);
      padding-block: calc((80 - 36) / 2 * var(--rem-ratio));
    }
  }

  .drawer-navigation__overlay {
  }
</style>
